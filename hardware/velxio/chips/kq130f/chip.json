{
  "schema": "velxio-chip/v1",
  "name": "KQ-130F (xKoin)",
  "author": "xKoin",
  "license": "MIT",
  "description": "KQ-130F narrowband power line carrier module. 9600 8N1 UART on TX/RX plus a synthetic LINE pin that is the mains: bytes arriving on RX are framed onto LINE, and every other KQ-130F on the same LINE net replays them on its TX once the frame completes.",
  "pins": [
    "TX",
    "RX",
    "LINE",
    "VCC",
    "GND"
  ],
  "attributes": [
    { "name": "line_noise_percent", "label": "Line noise (%)", "type": "int", "default": 0, "min": 0, "max": 100 },
    { "name": "bit_period_us", "label": "Line bit period (us)", "type": "int", "default": 200, "min": 10, "max": 200000 },
    { "name": "label", "label": "Label", "type": "string", "default": "kq130f" }
  ]
}
