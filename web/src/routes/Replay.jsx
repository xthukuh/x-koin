import Placeholder from './Placeholder.jsx';

export default function Replay() {
  return (
    <Placeholder
      outline={[
        'End-to-end journeys from Node setup to a Client paying for bytes',
        'Every payload, contract call and signature check as an expandable proof, open by default',
        'On-chain records from the proof run as evidence',
        'Threat-actor scenarios and how the system refuses, defends or permits',
      ]}
    />
  );
}
