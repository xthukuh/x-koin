/**
 * The /docs route. Everything it is made of lives in src/docs/, so this file
 * stays a one-line mount point that App.jsx can keep importing unchanged.
 */
export { default } from '../docs/Docs.jsx';
